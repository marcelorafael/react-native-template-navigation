/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';

const APP_NAME = "ibpDashboardApp";

AppRegistry.registerComponent(APP_NAME, () => App);
